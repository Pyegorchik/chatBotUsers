version: '3.8'

services:
  telegram-bot:
    build: .
    container_name: telegram-chat-parser-bot
    environment:
      - TELEGRAM_BOT_TOKEN=${TELEGRAM_BOT_TOKEN}
    restart: unless-stopped
    volumes:
      - ./logs:/root/logs